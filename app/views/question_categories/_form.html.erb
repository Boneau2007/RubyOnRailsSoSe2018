<%= form_with(model: question_category, local: true) do |form| %>
  <% if question_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_category.errors.count, "error") %> prohibited this question_category from being saved:</h2>

      <ul>
      <% question_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.text_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :categorie_id %>
    <%= form.text_field :categorie_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
